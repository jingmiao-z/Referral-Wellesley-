

# def insert_referrer_db(){
#     '''insert a new referrer user into database'''
#     sql = "INSERT INTO student (tt, title, `release`) VALUES (%s,%s,%s);"
#     curs.execute(sql,[tt,title,release])
#     conn.commit()
# }

# def insert_admin_db(){
#     '''insert a new admin user into database'''
#     sql = "INSERT INTO admin (tt, title, `release`) VALUES (%s,%s,%s);"
#     curs.execute(sql,[tt,title,release])
#     conn.commit()
# }